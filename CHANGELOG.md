# 更新日志

本项目使用[Semantic Versioning](https://semver.org/lang/zh-CN/)管理版本。

## [1.1.1] - 2026-09-10

### 新增

- 轻量视觉 Harness：最小区域截图、固定 Prompt、严格 JSON 契约、本地确定性判定、结果缓存和 Allure 脱敏证据。
- 视觉断言 `vision_contains`、`vision_count`、`vision_page_state` 和 `vision_canvas_ready`。
- 视觉基线断言 `vision_compare_reference`：按用例 ID 自动读取标准截图，在浏览器内存中自动合成单张上下对照图后与当前界面进行语义比较；兼容单图网关，标准图缺失时失败且永不自动覆盖。
- 步骤视觉比较 `vision_compare_step`：支持在同一 Excel 用例的点击序列中逐步截图，按受治理的红框坐标裁剪并比较多个中间状态；`vision_step_sequence` 校验比较次数完整。
- 视觉断言强制使用独立「断言定位器」，避免整页截图与无关客户信息外发。
- 视觉基线元数据契约：校验标准图 SHA-256、尺寸、截图定位器和人工批准状态，阻止未经审核的图片替换。
- 项目专用 `vision-eval` Skill 与评测运行器：支持离线数据门禁、正反样本准确率、缺陷召回率、Schema/服务错误率、延迟及无缓存重复一致性报告。

### TestOps 发布

- Web 自动化包版本为 `1.1.1`，Excel 基线版本为 `case-v1.0.4`。
- 基线包含 98 条用例，其中 93 条启用，并新增 `TC-IMAGE-035` 至 `TC-IMAGE-042`；`TC-IMAGE-042` 暂时禁用。
- 公开仓库不包含真实人脸标准图和对应标注集；完成脱敏或迁移到受控私有存储后再启用 `TC-IMAGE-042`。
- 发布工作流使用固定 TOP 适配器提交校验视觉操作与断言，再构建、签名并发布不可变 Runner 镜像及基线资产。

## [1.1.0] - 2026-09-08

### 发布说明

- 仅发布了 Excel 基线更新标签；该标签未包含完整视觉执行实现和 TestOps Release 资产，不作为当前平台正式包使用。

## [1.0.0] - 2026-08-31

### 新增

- 基于 Playwright、pytest 和 Allure 的 Excel 数据驱动 Web 自动化框架。
- 单账号、双账号双进程与 `SERIAL` 串行用例执行。
- 启动前用例校验、失败截图、Excel 结果回写和 Allure 报告归档。
- 失败用例自动复跑与双进程结果汇总。
- 登录态复用、中文浏览器环境与测试环境影像页兼容配置。
- 离线单元测试、用例编写规范和发布流程文档。

### 发布边界

- `test_cases/test_case.xlsx` 是 v1.0.0 的用例基线。
- 视觉模型客户端与视觉断言仍属后续功能，不属于 v1.0.0 正式承诺范围。
- 本地运行结果、报告、截图、密钥和 Excel 备份不属于发布产物。

### 发布验证

- 2026-08-31 离线单元测试：23 项全部通过。
- 2026-08-31 双账号全量回归：93 条启用用例全部通过，0 failed、0 broken、0 skipped。
